<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (C) 2026 Vertel Sverige AB - License AGPL-3.0 -->
<odoo>
    <record id="view_keykeep_settings" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.keykeep</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base_setup.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Keykeep" string="Keykeep"
                     name="keykeep" data-icon="fa-key"
                     groups="keykeep.group_manager">

                    <block title="Background Jobs">
                        <setting id="keykeep_bg_jobs_display" string="Cron-jobb">
                            <field name="keykeep_cron_line_ids" widget="one2many">
                                <form>
                                    <group>
                                        <field name="cron_id" readonly="1" force_save="1"/>
                                        <field name="cron_name" readonly="1"/>
                                        <field name="cron_active"/>
                                        <field name="cron_interval_number"/>
                                        <field name="cron_interval_type"/>
                                        <field name="cron_lastcall" readonly="1"/>
                                        <field name="cron_failure_count" readonly="1"/>
                                        <field name="cron_code" readonly="1"/>
                                        <button name="action_run_now" type="object"
                                                string="Kör nu" icon="fa-play"/>
                                    </group>
                                </form>
                                <list>
                                    <field name="cron_name"/>
                                    <field name="cron_active"/>
                                    <field name="cron_interval_number"/>
                                    <field name="cron_interval_type"/>
                                    <field name="cron_lastcall"/>
                                    <field name="cron_failure_count"/>
                                </list>
                            </field>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <!-- Settings action + meny -->
    <record id="action_keykeep_settings" model="ir.actions.act_window">
        <field name="name">Keykeep Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module': 'keykeep', 'bin_size': False}</field>
    </record>

</odoo>
